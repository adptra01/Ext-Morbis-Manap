/**
 * Global type declarations for build-time injected constants.
 * esbuild `define` replaces these with literal strings at build time.
 */

/** Compiled Tailwind CSS injected into resumeTab / resumeRanapTab bundles. */
declare const SHADOW_CSS: string | undefined;
